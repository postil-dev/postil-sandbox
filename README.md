Sandbox repo for exercising Postil PR reviews.

## Hosted queue-state probe

This temporary change verifies whether Postil exposes the queued Trigger run id
on the hosted review check before the worker completes.
